#!/usr/bin/env zsh

for f in "$@"
do
  dir="${f:h}" # dirname
  ext="${f:t:e}" # file extension (without dot)
  old="${f:t:r}" # old basename
  new="$(kebab-case "${old}")" # new basename
  if [[ "${old}" != "${new}" ]]; then
    mv -iv "${f}" "${dir}/${new}.${ext}"
  fi
done

